# 更新记录

## 1.3.0

- 「拉取模型」优先调用酒馆自带的 chat-completions/status 服务端代理（由酒馆服务端请求 {接口地址}/models，无跨域、不经过反代），失败时自动回退到反代/直连
- 提炼「接口地址 → 模型列表地址」推导与模型 id 收集为公共函数

## 1.2.0

- 重写 README，按当前界面与流程整理文档
- 清理代码中残留的旧注释与无用函数

## 1.1.2

- OpenAI 兼容渠道支持透传 NovelAI 参数（negative_prompt / width / height / steps / scale / sampler / n_samples / noise_schedule），实测本中转站会透传
- 参数组在 OpenAI 兼容模式下也显示

## 1.1.1

- 临时回退 1.1.0 新增的参数透传（当时误判为中转站参数报错）

## 1.1.0

- 更名为「桃桃绘图」
- 去掉「运行模式」板块：不再让用户选择服务端 / 直连
- 渠道区分：NovelAI 官方直连不走代理；OpenAI 兼容 / 通用 JSON 走固定反代
- 反代地址固定写死（PROXY_PREFIX），面板不可编辑
- 去掉「模型列表地址」输入框，改为从接口地址自动推导

## 1.0.0

- 首个版本：扩展 + Node 服务端插件双形态
- 自动读取 <@tags> 生图、写回 tag 美化正则、一键安装配套资源
